<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.kuaikan.ads.kyle.account.potential.dao.AdPotentialAccountMapper">
    <resultMap id="resultMap" type="com.kuaikan.ads.kyle.account.potential.model.AdPotentialAccount">
        <result column="id" property="id" jdbcType="BIGINT"/>
        <result column="company_name" property="companyName" jdbcType="VARCHAR"/>
        <result column="source_app" property="sourceApp" jdbcType="VARCHAR"/>
        <id column="id" property="id" jdbcType="BIGINT"/>
    </resultMap>

    <sql id="TABLE_NAME">
        `ad_potential_account`
    </sql>

    <sql id="INSERT_COLS">
        `id`,
        `company_name`,
        `source_app`,
        `id`
    </sql>

    <sql id="INSERT_VALUES">
        0
        #{id},
        #{companyName},
        #{sourceApp},
        #{id
    </sql>

    <insert id="insert" parameterType="com.kuaikan.ads.kyle.account.potential.model.AdPotentialAccount" useGeneratedKeys="true"
            keyProperty="id">
        insert into
        <include refid="TABLE_NAME"/>(<include refid="INSERT_COLS"/>)
        values(<include refid="INSERT_VALUES"/>)
    </insert>

    <sql id="WHERE_ID">
        <where>
            `id` = '#{id}'
        </where>
    </sql>

    <update id="update" parameterType="com.kuaikan.ads.kyle.account.potential.model.AdPotentialAccount">
        update <include refid="TABLE_NAME"/>
        <set>
            `id` = #{id},
            `company_name` = #{companyName},
            `source_app` = #{sourceApp},
            `id` = #{id}
        </set>
        <include refid="WHERE_ID"/>
    </update>

    <sql id="LIST_WHERE">
        <if test="sourceApp != null}">
            and source_app='#{sourceApp}'
        </if>
    </sql>

    <select id="list" resultMap="resultMap">
        select *
        from  <include refid="TABLE_NAME"/>
        <include ref="LIST_WHERE"/>
        order by id
        <if test="size < 0">
            limit #{start},#{size}
        </if>
    </select>

    <select id="count" resultType="long">
        select count(1)
        from  <include refid="TABLE_NAME"/>
        <include ref="LIST_WHERE"/>
    </select>
</mapper>